# Full-Stack Audit & Release — 2026-07-03

Scope: all 3 mobile apps (customer/rider/store, iOS + Android), admin console, backend.
Deployed: Cloud Run api rev `00007-bl6`, admin rev `00007-mpr`; all 6 EAS builds queued
with auto-submit (Play internal + TestFlight).

## Requested changes

| # | Change | Where |
|---|--------|-------|
| 1 | OTP login is the DEFAULT on login screens (password behind a toggle) | customer, rider, store apps |
| 2 | Order quantity in **kg + grams** with weight-based pricing | admin New Order, backend, invoices, drawer, bill editor |
| 3 | No lat/long asked when creating a customer/delivery order | admin New Order UI + backend validation |
| 4 | Sorting & item categorization | backend `/services` sort, customer category browsing, admin services search |

## Security fixes (found by audit — were live in prod)

1. **Admin login page printed real admin credentials** (`+919999999999 / Test@1234`) at
   admin.washingbells.com → removed. **Rotate this password.**
2. **Unauthenticated `POST /admin/seed-admin`** → deleted (returns 404 in prod now).
3. **OTP dev bypass was implicit**: missing Twilio creds silently made `123456` a universal
   password for ANY phone. Now an explicit `OTP_DEV_BYPASS` env flag (default **fail closed**).
   Prod currently runs `OTP_DEV_BYPASS=true` on purpose (test phase, no Twilio yet).
4. **Payment verify was fail-open** without Razorpay keys → now only accepts dev-mock order ids.
5. **No OTP rate limiting** → 25s per-phone resend cooldown + 6/hour cap (`otp_requests`, 1h TTL).
6. **CORS `*` with credentials** → restricted to admin./www./washingbells.com + localhost dev.
7. Rider/store apps showed "DEV: Use OTP 123456" in production builds → gated behind `__DEV__`.
8. `TEST_CREDENTIALS.md`/`TEST_INFO.md` untracked from git (kept locally, gitignored);
   dead `.bak`/scratch files deleted.

## Feature work

**Weight-based orders (kg/grams):** services with `pricing_unit: kg` (prod has 3: Premium
Laundry, Wash & Fold, Wash and Iron) take fractional quantities. Admin New Order shows
kg+g inputs, price computed per kg; line items store `unit`; garment tags emit ONE tag per
kg line (it's a bag); invoice PDF prints `3.25 kg` / `₹79/kg`; orders drawer + bill editor
kg-aware; customer app shows `/kg` price labels and `x N kg` on order lines.
Verified on prod: order `WB-2026-O11T` — 3.25 kg @ ₹79 = ₹256.75 + ₹40 delivery, 1 tag.

**Coordinates optional:** rider-delivery orders need only the address text (riders navigate
by address string; map pin falls back to store coords). Admin UI lat/long inputs removed.
The standalone Add Customer modal never needed coords (verified).

**Sorting/categorization:** `/services` + `/admin/services` now return items sorted by
canonical category order then name (fixes YAML/$push insertion-order drift everywhere).
Customer home now renders the "Shop by Category" tiles (component existed but was never
mounted); category pages use the shared `matches()` so Men/Women/Kids include unisex items
(strict equality was hiding Shirt/Jeans/Kurta from "Men"); admin Services page has an item
search that auto-expands matching services.

**App auth/UX:** OTP-first on all 3 login screens; rider/store OTP screens gained
Resend-with-30s-countdown (had none); rider/store now persist refresh tokens; customer
OTP login registers push notifications (was password-login only); customer orders tab has
a real error + Retry state instead of a misleading "No orders yet" on network failure.

**Store-listing hygiene:** `RECORD_AUDIO` removed from rider/store (Play rejection risk);
expo-image-picker configured with camera/photo permission strings and `microphonePermission:
false` (also fixes iOS crash-on-camera without NSCameraUsageDescription); customer Android
permission duplicates removed.

## Deploy state

- **Backend**: Cloud Run `washingbells-api` rev 00007-bl6 (env `OTP_DEV_BYPASS=true` added).
- **Admin**: Cloud Run `washingbells-admin` rev 00007-mpr.
- Verified live: health OK; sorted items; OTP 200→429 cooldown; seed-admin 404; login page
  clean; OTP 123456 login works (explicit bypass); kg order without coords created.
- **Mobile — ALL 6 SHIPPED**: iOS builds for customer (build 12) / rider / store (build 4)
  submitted to TestFlight; Android builds for all three submitted to Play internal testing.
  All submissions FINISHED.
  - Build gotcha #1: rider/store archives were empty because the root `.easignore`
    (customer scope) excludes `rider/`+`store/` and both dirs are now tracked by the parent
    repo → committed `.easignore-rider`/`.easignore-store` variants; swap in before building.
  - Build gotcha #2: an untracked 406MB native `android/` scratch dir (from a local
    `expo run:android`) uploaded with customer archives flipped EAS into a broken bare
    Android build ("No variants exist" in Gradle) → root `.easignore` now excludes
    `/android/` + `/ios/` so EAS prebuilds managed native code.

## Before REAL launch (still open)

- Wire Twilio Verify secrets (`TWILIO_ACCOUNT_SID/AUTH_TOKEN/VERIFY_SERVICE_SID`) and
  **remove `OTP_DEV_BYPASS`** from Cloud Run env.
- Rotate the admin password (`Test@1234` was exposed on the public login page).
- Razorpay live keys; Atlas network lockdown; delete `is_dummy` seed rows and audit-test
  customers `+919899000011` / `+919899000012`.

---

# Round 2 — four field bugs (same day)

1. **Rider "test OTP"** → the generate-OTP endpoints returned `dev_otp` straight to the
   rider, who read it off their own alert. Now: OTP goes to the CUSTOMER only (push + a
   green "Pickup/Delivery OTP" card on their order screen); rider types what the customer
   tells them. Verified E2E via API: generate → response has no code → customer fetch shows
   it → wrong code 400 → right code delivers.
2. **Invoices not downloading** → prod URL in `lib/invoice.js` + `store/lib/printTags.js`
   was `api.washingbells.in` (domain doesn't exist). Fixed to `.com` (also un-broke garment
   tag printing). Walk-in "Print Bill" no longer swallows errors.
3. **Pay on delivery broken** → order-screen Pay Now was a dev stub posting a fabricated
   signature (always failed in prod, then hid itself). Now opens the real Razorpay WebView
   (same as checkout), shown for ANY unpaid order at any stage; `/payments/verify` no longer
   regresses fulfilment status when paying mid-delivery; rider delivery completion marks
   COD orders paid ("Payment collected on delivery"); rider sees a "Collect ₹X" banner.
4. **Admin emails** → `EMAIL_ADMIN_ADDRESS` is now a comma list
   (`admin@washingbells.com,hardik.vashisht008@gmail.com` on Cloud Run). New seeded events:
   `new_order_admin` (all 3 create paths), `order_ready_admin`, `order_delivered_admin`
   (rider + counter), `weekly_summary_admin`. Weekly summary: Cloud Scheduler job
   `wb-weekly-summary` (Mon 09:00 IST, asia-south1) → `POST /admin/reports/weekly-email-cron?key=`
   (WEEKLY_REPORT_KEY env; local copy in `.weekly-report-key.local`, gitignored). Console
   "send now": `POST /admin/reports/weekly-email` (admin JWT). Live-tested: both recipients,
   real aggregates, 403 on bad key.

Deploy: api rev **00008-n56**. Admin console unchanged this round. All 6 mobile builds
re-queued with auto-submit (Play internal + TestFlight).

---

# Round 3 — promo bug, cash collection, wallet + sweep (same day)

1. **Promo blocked ordering** → percent coupons saved without a max-discount cap store
   `max_discount: None`; order-create's duplicate calculator did `min(x, None)` → 500.
   Now delegates to the shared `evaluate_coupon` (same rules as /coupons/validate and the
   admin console); invalid coupons 400 instead of silently dropping the discount.
   Verified: no-cap 10% coupon order placed (₹750 − ₹75 = ₹675).
2. **Cart didn't clear after ordering** → server clears it; the app never resynced.
   Checkout now resets local cart state; basket refetches on every focus.
3. **COD cash collection** → new `POST /delivery/{trip}/collect-payment` + prominent rider
   "Cash Collected — ₹X" button (confirm dialog). Marks paid, pushes + emails the customer
   (`payment_collected`), emails both admins (`payment_collected_admin`). Idempotent.
   Auto-settle on delivery-OTP verify remains as a backstop.
4. **Sweep findings fixed** (12-point functional sweep):
   - Wallet top-up NEVER worked (fake dev payload) → real Razorpay WebView; verify now
     credits the server-stored intent amount (client-claimed amounts were exploitable) and
     is idempotent. Verified: ₹250 credited, claimed 999999 ignored.
   - Rider background GPS hit dead `api.washingbells.in` → `.com` (tracking died off the
     home screen in prod builds). ALL remaining `.in` refs fixed (support emails, invite &
     referral URLs).
   - Prepaid orders skipped the store's Accept/Reject (payment flipped status to
     "confirmed") → payment now only marks paid.
   - Store Reject stranded customer money → wallet refunded, coupon use released, paid
     orders flagged `needs_manual_refund` in admin notifications.
   - Store assign/book rider now pushes the rider (backgrounded riders never learned).
   - Rider photo upload: 120s timeout + 10-photo cap; reorder rounds kg quantities.
5. **Play internal testing "no updates"** → distribution, not pipeline: track verified live
   via the publisher API. Play does NOT notify for internal builds — testers must be in the
   tester list and opted in via the opt-in link, then the update appears in Play Store.

Deploy: api rev **00009-hh5**; all 6 mobile builds re-queued with auto-submit.

**Deferred (feature-sized, not bugs):** referral-code entry UI (backend ready, no screen),
"choose different store" after rejection is a dead end (cart already consumed), ratings
end-to-end (schema exists, no endpoint/UI), rider/store token auto-refresh (24h hard
logout), store Set-Time timezone + display.
